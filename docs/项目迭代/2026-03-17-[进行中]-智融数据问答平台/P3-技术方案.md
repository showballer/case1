---
需求名称: 智融数据问答平台
关联需求: P1-需求文档.md
创建时间: 2026-03-17
当前阶段: P3
文档版本: v1.1
---

# P3-技术方案：智融数据问答平台

## 一、方案概述

### 1.1 设计目标

基于 P1-需求文档，本技术方案旨在构建一个**企业级智能数据问答平台**，实现以下核心目标：

1. **自然语言数据查询**：用户通过自然语言完成数据查询，无需掌握 SQL
2. **智能编排能力**：支持通过自然语言生成和执行数据处理流程
3. **AI 报告生成**：基于主题和大纲自动生成分析报告
4. **统一知识管理**：沉淀业务知识和查询案例，支持智能召回
5. **企业级管理**：提供完整的后台管理能力

### 1.2 系统边界

| 范围 | 职责 | 说明 |
|------|------|------|
| **前端应用** | ✅ 自研 | Next.js + React 全家桶 |
| **业务后端** | ✅ 自研 | 业务逻辑、数据管理、用户管理 |
| **AI 能力** | ❌ 第三方提供 | 由 AI 团队独立提供，本系统调用 |
| **数据存储** | ✅ 自研 | MySQL 业务数据库 |

### 1.3 核心设计原则

| 原则 | 说明 |
|------|------|
| 前后端分离 | Next.js 前端 + 独立业务后端，清晰职责边界 |
| UI 一致性 | 统一采用 shadcn/ui 设计规范，水墨淡出风格 |
| 流式优先 | AI 交互采用 SSE 流式输出，打字机效果展示 |
| 接口解耦 | AI 能力通过接口调用，降低系统耦合度 |
| 安全合规 | 数据脱敏、权限控制、操作审计内置设计 |

### 1.4 技术栈选型

#### 1.4.1 前端技术栈

| 层级 | 技术选型 | 选型理由 |
|------|----------|----------|
| **框架** | Next.js 14 (App Router) | React 全栈框架，支持 SSR/SSG，优秀 DX |
| **UI 库** | React 18 | 声明式 UI，生态丰富 |
| **组件库** | shadcn/ui | 高度可定制，基于 Radix UI，符合水墨风格 |
| **样式方案** | Tailwind CSS | 原子化 CSS，快速实现水墨淡出风格 |
| **状态管理** | Zustand | 轻量级，支持 React 18 并发渲染 |
| **表单处理** | React Hook Form + Zod | 高性能表单，类型安全校验 |
| **图表库** | Recharts / ECharts | React 友好，支持水墨风格定制 |
| **富文本编辑** | Lexical / TipTap | 现代化编辑器，可扩展性强 |
| **流式处理** | 原生 EventSource + 自定义 Hook | SSE 流式输出，打字机效果 |

#### 1.4.2 后端技术栈

| 层级 | 技术选型 | 选型理由 |
|------|----------|----------|
| **运行时** | Node.js 20 LTS | 与前端统一技术栈，全栈 JavaScript |
| **框架** | NestJS | 企业级框架，模块化架构，TypeScript 原生支持 |
| **ORM** | Prisma | 类型安全，迁移管理便捷 |
| **数据库** | MySQL 8.0 | 开源免费，生态成熟，团队熟悉度高 |
| **认证** | JWT + Passport | 标准化认证方案 |
| **文件存储** | MinIO / 本地存储 | 对象存储，支持文件上传 |
| **API 文档** | Swagger / OpenAPI | 自动生成 API 文档 |

#### 1.4.3 DevOps

| 工具 | 用途 |
|------|------|
| Docker | 容器化部署 |
| Docker Compose | 本地开发环境 |
| GitHub Actions | CI/CD 流水线 |
| PM2 / Kubernetes | 生产环境进程管理 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              前端层 (Next.js 14)                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  智能问答     │  │  智能编排     │  │  AI报告      │  │  后台管理     │         │
│  │  /chat       │  │  /workflow   │  │  /report     │  │  /admin      │         │
│  │  shadcn/ui   │  │  shadcn/ui   │  │  shadcn/ui   │  │  shadcn/ui   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────────┐│
│  │  共享组件：流式输出(打字机)、图表、富文本编辑器、表格、表单                      ││
│  └─────────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐
│   业务后端 (NestJS)   │  │    AI 服务 (第三方)   │  │    业务数据源         │
│                      │  │                      │  │                      │
│ ┌──────────────────┐ │  │ ┌──────────────────┐ │  │ ┌──────────────────┐ │
│ │ 用户管理         │ │  │ │ 智能问答(WS)     │ │  │ │ MySQL            │ │
│ │ 数据源管理       │ │  │ │ CodeAct(SSE)     │ │  │ │ PostgreSQL       │ │
│ │ 知识管理         │ │  │ │ 报告生成(SSE)    │ │  │ │ TeleDB           │ │
│ │ 工作流管理       │ │  │ │ 节点运行(SSE)    │ │  │ │ DM(达梦)         │ │
│ │ 报告管理         │ │  │ │ 智能填充(HTTP)   │ │  │ │ 本地文件         │ │
│ │ 反馈管理         │ │  │ │ 联网查询(HTTP)   │ │  │ └──────────────────┘ │
│ │ 监控统计         │ │  │ └──────────────────┘ │  │                      │
│ └──────────────────┘ │  │                      │  └──────────────────────┘
│                      │  └──────────────────────┘
│ ┌──────────────────┐ │
│ │ MySQL            │ │
│ └──────────────────┘ │
└──────────────────────┘
```

### 2.2 目录结构设计

```
zhirong-platform/
├── apps/
│   ├── web/                              # Next.js 前端应用
│   │   ├── src/
│   │   │   ├── app/                      # App Router 页面
│   │   │   │   ├── (main)/               # 主布局
│   │   │   │   │   ├── chat/             # 智能问答
│   │   │   │   │   ├── workflow/         # 智能编排
│   │   │   │   │   ├── report/           # AI报告
│   │   │   │   │   └── admin/            # 后台管理
│   │   │   │   ├── layout.tsx
│   │   │   │   └── page.tsx
│   │   │   ├── components/               # 组件
│   │   │   │   ├── ui/                   # shadcn/ui 组件
│   │   │   │   ├── chat/                 # 问答组件
│   │   │   │   ├── workflow/             # 编排组件
│   │   │   │   ├── report/               # 报告组件
│   │   │   │   ├── admin/                # 后台组件
│   │   │   │   └── shared/               # 共享组件
│   │   │   │       ├── stream-output.tsx # 打字机效果组件
│   │   │   │       ├── chart.tsx         # 图表组件
│   │   │   │       └── rich-editor.tsx   # 富文本编辑器
│   │   │   ├── hooks/                    # 自定义 Hooks
│   │   │   │   ├── use-stream.ts         # SSE 流式处理
│   │   │   │   ├── use-websocket.ts      # WebSocket 连接
│   │   │   │   └── use-typewriter.ts     # 打字机效果
│   │   │   ├── stores/                   # Zustand 状态管理
│   │   │   ├── lib/                      # 工具函数
│   │   │   │   ├── api/                  # API 客户端
│   │   │   │   ├── ai/                   # AI 接口封装
│   │   │   │   └── utils.ts              # 通用工具
│   │   │   └── styles/                   # 全局样式
│   │   │       └── ink-wash.css          # 水墨风格样式
│   │   ├── public/
│   │   ├── next.config.js
│   │   ├── tailwind.config.js
│   │   └── package.json
│   │
│   └── server/                           # NestJS 后端应用
│       ├── src/
│       │   ├── modules/                  # 业务模块
│       │   │   ├── user/                 # 用户模块
│       │   │   ├── datasource/           # 数据源模块
│       │   │   ├── knowledge/            # 知识管理模块
│       │   │   ├── workflow/             # 工作流模块
│       │   │   ├── report/               # 报告模块
│       │   │   ├── feedback/             # 反馈模块
│       │   │   └── monitor/              # 监控模块
│       │   ├── common/                   # 公共模块
│       │   │   ├── decorators/           # 装饰器
│       │   │   ├── filters/              # 异常过滤器
│       │   │   ├── guards/               # 守卫
│       │   │   ├── interceptors/         # 拦截器
│       │   │   └── pipes/                # 管道
│       │   ├── config/                   # 配置
│       │   ├── prisma/                   # Prisma Schema
│       │   └── main.ts
│       ├── prisma/
│       │   └── schema.prisma
│       └── package.json
│
├── packages/                             # 共享包
│   ├── types/                            # TypeScript 类型定义
│   └── eslint-config/                    # ESLint 配置
│
├── docker/
│   ├── Dockerfile.web
│   ├── Dockerfile.server
│   └── docker-compose.yml
│
├── docs/
│   └── api/                              # API 文档
│
├── turbo.json                            # Turborepo 配置
└── package.json                          # Monorepo 根配置
```

---

## 三、UI/UX 设计规范

### 3.1 shadcn/ui 组件规范

基于 shadcn/ui 实现水墨淡出风格，核心设计语言：

| 设计元素 | 规范 |
|----------|------|
| **色彩** | 水墨灰阶为主，淡蓝/淡青作为强调色 |
| **圆角** | 中等圆角 (rounded-md/lg) |
| **阴影** | 柔和阴影，淡出效果 |
| **动画** | 淡入淡出过渡，流畅自然 |
| **字体** | 思源黑体 / Noto Sans SC |

### 3.2 水墨风格色板

```css
/* tailwind.config.js 扩展 */
colors: {
  ink: {
    50: '#fafafa',   /* 淡墨白 */
    100: '#f5f5f5',  /* 浅灰 */
    200: '#e5e5e5',  /* 中浅灰 */
    300: '#d4d4d4',  /* 灰 */
    400: '#a3a3a3',  /* 中灰 */
    500: '#737373',  /* 墨灰 */
    600: '#525252',  /* 深灰 */
    700: '#404040',  /* 暗灰 */
    800: '#262626',  /* 墨黑 */
    900: '#171717',  /* 浓墨 */
  },
  accent: {
    light: '#e0f2fe', /* 淡青 */
    DEFAULT: '#0ea5e9', /* 青蓝 */
    dark: '#0369a1',   /* 深青 */
  }
}
```

### 3.3 流式输出与打字机效果

**SSE 流式处理 Hook：**

```typescript
// hooks/use-stream.ts
import { useEffect, useState, useCallback } from 'react';

interface StreamOptions {
  onChunk: (chunk: string) => void;
  onComplete: () => void;
  onError: (error: Error) => void;
}

export function useStream() {
  const [isStreaming, setIsStreaming] = useState(false);

  const startStream = useCallback(async (url: string, options: StreamOptions) => {
    setIsStreaming(true);
    try {
      const response = await fetch(url, { method: 'POST' });
      const reader = response.body?.getReader();
      const decoder = new TextDecoder();

      while (reader) {
        const { done, value } = await reader.read();
        if (done) break;
        const chunk = decoder.decode(value);
        options.onChunk(chunk);
      }
      options.onComplete();
    } catch (error) {
      options.onError(error as Error);
    } finally {
      setIsStreaming(false);
    }
  }, []);

  return { isStreaming, startStream };
}
```

**打字机效果组件：**

```typescript
// components/shared/stream-output.tsx
'use client';

import { useEffect, useState, useRef } from 'react';
import { cn } from '@/lib/utils';

interface StreamOutputProps {
  content: string;
  speed?: number;  // 打字速度 (ms/字符)
  className?: string;
  isStreaming?: boolean;
}

export function StreamOutput({
  content,
  speed = 30,
  className,
  isStreaming = false
}: StreamOutputProps) {
  const [displayedContent, setDisplayedContent] = useState('');
  const cursorRef = useRef<HTMLSpanElement>(null);

  useEffect(() => {
    if (!isStreaming) {
      setDisplayedContent(content);
      return;
    }

    let index = 0;
    const timer = setInterval(() => {
      if (index < content.length) {
        setDisplayedContent(content.slice(0, index + 1));
        index++;
      } else {
        clearInterval(timer);
      }
    }, speed);

    return () => clearInterval(timer);
  }, [content, isStreaming, speed]);

  return (
    <div className={cn('font-mono whitespace-pre-wrap', className)}>
      {displayedContent}
      {isStreaming && (
        <span
          ref={cursorRef}
          className="inline-block w-2 h-4 bg-ink-600 animate-pulse ml-0.5"
        />
      )}
    </div>
  );
}
```

### 3.4 淡出动画规范

```css
/* 水墨淡出动画 */
@keyframes ink-fade-in {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes ink-fade-out {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

.animate-ink-fade-in {
  animation: ink-fade-in 0.3s ease-out forwards;
}

.animate-ink-fade-out {
  animation: ink-fade-out 0.2s ease-in forwards;
}
```

---

## 四、AI 接口对接

### 4.1 AI 接口清单

| 序号 | 接口名称 | 接口路径 | 类型 | 说明 |
|------|----------|----------|------|------|
| 1 | 智能数据问答 | WebSocket | 改造 | 新增 case_data 事件返回案例数据 |
| 2 | CodeAct分析 | POST /api/codeact/sessions/{id}/analyze | 新增 | SSE 流式 |
| 3 | CodeAct文件上传 | POST /api/codeact/sessions/{id}/upload | 新增 | 普通 HTTP |
| 4 | 报告大纲生成 | POST /api/reports/{id}/outline | 新增 | SSE 流式 |
| 5 | 报告内容生成 | POST /api/reports/{id}/generate | 新增 | SSE 流式 |
| 6 | 节点运行 | POST /api/workflows/node/execute | 新增 | SSE 流式 |
| 7 | 智能填充元数据 | POST /api/datasources/{id}/tables/{name}/smart-fill | 已有 | 普通 HTTP |
| 8 | 联网查询知识 | POST /api/knowledge/business/online-search | 已有 | 普通 HTTP |
| 9 | 数据源启停通知 | POST /api/ai/datasource/notify | 待确认 | 普通 HTTP |

### 4.2 AI 接口封装

```typescript
// lib/ai/client.ts
import { useStream } from '@/hooks/use-stream';

const AI_BASE_URL = process.env.NEXT_PUBLIC_AI_API_URL;

export const aiClient = {
  // WebSocket 问答
  chat: {
    connect: (sessionId: string) => {
      const ws = new WebSocket(`${AI_BASE_URL.replace('http', 'ws')}/ws/chat`);
      ws.onmessage = (event) => {
        const data = JSON.parse(event.data);
        // 处理不同事件类型: message, case_data, error
      };
      return ws;
    }
  },

  // SSE 流式接口
  codeact: {
    analyze: (sessionId: string, body: any) =>
      fetch(`${AI_BASE_URL}/api/codeact/sessions/${sessionId}/analyze`, {
        method: 'POST',
        body: JSON.stringify(body)
      })
  },

  report: {
    generateOutline: (reportId: string, body: any) =>
      fetch(`${AI_BASE_URL}/api/reports/${reportId}/outline`, {
        method: 'POST',
        body: JSON.stringify(body)
      }),
    generateContent: (reportId: string, body: any) =>
      fetch(`${AI_BASE_URL}/api/reports/${reportId}/generate`, {
        method: 'POST',
        body: JSON.stringify(body)
      })
  },

  workflow: {
    executeNode: (body: any) =>
      fetch(`${AI_BASE_URL}/api/workflows/node/execute`, {
        method: 'POST',
        body: JSON.stringify(body)
      })
  },

  // 普通 HTTP 接口
  datasource: {
    smartFill: (datasourceId: string, tableName: string) =>
      fetch(`${AI_BASE_URL}/api/datasources/${datasourceId}/tables/${tableName}/smart-fill`, {
        method: 'POST'
      })
  },

  knowledge: {
    onlineSearch: (query: string) =>
      fetch(`${AI_BASE_URL}/api/knowledge/business/online-search`, {
        method: 'POST',
        body: JSON.stringify({ query })
      })
  }
};
```

### 4.3 WebSocket 问答封装

```typescript
// hooks/use-chat-websocket.ts
import { useEffect, useRef, useState, useCallback } from 'react';

interface ChatMessage {
  type: 'message' | 'case_data' | 'error' | 'done';
  content?: string;
  data?: any;
}

export function useChatWebSocket(url: string) {
  const wsRef = useRef<WebSocket | null>(null);
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [isConnected, setIsConnected] = useState(false);

  const connect = useCallback(() => {
    wsRef.current = new WebSocket(url);

    wsRef.current.onopen = () => setIsConnected(true);
    wsRef.current.onclose = () => setIsConnected(false);

    wsRef.current.onmessage = (event) => {
      const message: ChatMessage = JSON.parse(event.data);
      setMessages(prev => [...prev, message]);
    };
  }, [url]);

  const send = useCallback((data: any) => {
    if (wsRef.current?.readyState === WebSocket.OPEN) {
      wsRef.current.send(JSON.stringify(data));
    }
  }, []);

  const disconnect = useCallback(() => {
    wsRef.current?.close();
  }, []);

  return { messages, isConnected, connect, send, disconnect };
}
```

---

## 五、功能模块设计

### 5.1 模块总览

| 模块编号 | 模块名称 | 前端实现 | 后端实现 | AI 依赖 | 优先级 |
|----------|----------|----------|----------|---------|--------|
| M01 | 智能问答模块 | ✅ | ✅ | WebSocket | P0 |
| M02 | 智能编排模块 | ✅ | ✅ | SSE | P1 |
| M03 | AI报告模块 | ✅ | ✅ | SSE | P1 |
| M04 | 数据源管理模块 | ✅ | ✅ | HTTP(智能填充) | P0 |
| M05 | 知识管理模块 | ✅ | ✅ | HTTP(联网查询) | P0 |
| M06 | 用户管理模块 | ✅ | ✅ | - | P0 |
| M07 | 服务监控模块 | ✅ | ✅ | - | P1 |
| M08 | 反馈管理模块 | ✅ | ✅ | - | P1 |

### 5.2 模块详细设计

#### 5.2.1 M01 智能问答模块

**前端组件结构：**

```
chat/
├── page.tsx                    # 问答页面
├── components/
│   ├── session-list.tsx        # 会话列表
│   ├── message-list.tsx        # 消息列表
│   ├── message-item.tsx        # 单条消息
│   ├── input-box.tsx           # 输入框
│   ├── sub-question-card.tsx   # 子问题卡片
│   ├── result-table.tsx        # 结果表格
│   ├── chart-display.tsx       # 图表展示
│   └── feedback-dialog.tsx     # 反馈弹窗
└── hooks/
    └── use-chat.ts             # 问答逻辑
```

**核心交互流程：**

1. 用户输入问题 → WebSocket 发送
2. 接收流式消息 → 打字机效果展示
3. 复杂问题拆解 → 子问题卡片确认
4. 结果展示 → 表格 + 图表
5. 反馈收集 → 正向/负向反馈

#### 5.2.2 M02 智能编排模块

**前端组件结构：**

```
workflow/
├── page.tsx                    # 编排页面
├── components/
│   ├── workflow-editor.tsx     # 流程编辑器
│   ├── node-palette.tsx        # 算子面板
│   ├── node-item.tsx           # 算子节点
│   ├── node-config.tsx         # 节点配置
│   ├── execution-panel.tsx     # 执行面板
│   └── stream-output.tsx       # 流式输出
└── hooks/
    └── use-workflow.ts         # 编排逻辑
```

**关键特性：**
- React Flow 实现可视化编排
- SSE 流式执行节点
- 打字机效果展示执行结果

#### 5.2.3 M03 AI报告模块

**前端组件结构：**

```
report/
├── page.tsx                    # 报告页面
├── components/
│   ├── report-list.tsx         # 报告列表
│   ├── outline-editor.tsx      # 大纲编辑器
│   ├── rich-editor.tsx         # 富文本编辑器
│   ├── config-panel.tsx        # 配置面板
│   └── generate-progress.tsx   # 生成进度
└── hooks/
    └── use-report.ts           # 报告逻辑
```

**关键特性：**
- 拖拽排序大纲 (dnd-kit)
- 富文本编辑 (TipTap/Lexical)
- SSE 流式生成内容

---

## 六、数据模型

### 6.1 Prisma Schema

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(uuid())
  username     String   @unique
  passwordHash String
  name         String
  department   String?
  departmentId String?
  role         Role     @default(USER)
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  sessions   Session[]
  reports    Report[]
  feedbacks  Feedback[]

  @@map("users")
}

enum Role {
  USER
  ADMIN
}

model Session {
  id        String   @id @default(uuid())
  userId    String
  title     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user     User      @relation(fields: [userId], references: [id])
  messages Message[]

  @@map("sessions")
}

model Message {
  id        String   @id @default(uuid())
  sessionId String
  role      String   // user, assistant
  content   String   @db.Text
  sql       String?  @db.Text
  metadata  Json?
  createdAt DateTime @default(now())

  session   Session    @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  feedbacks Feedback[]

  @@map("messages")
}

model DataSource {
  id             String   @id @default(uuid())
  name           String
  type           String   // mysql, postgresql, teledb, dm, file
  connectionConfig Json   @db.Json
  isActive       Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  tables    DataSourceTable[]
  workflows Workflow[]
  reports   Report[]

  @@map("data_sources")
}

model DataSourceTable {
  id           String   @id @default(uuid())
  datasourceId String
  tableName    String
  tableComment String?
  metadata     Json?
  createdAt    DateTime @default(now())

  datasource DataSource  @relation(fields: [datasourceId], references: [id], onDelete: Cascade)
  fields     TableField[]

  @@map("data_source_tables")
}

model TableField {
  id           String   @id @default(uuid())
  tableId      String
  fieldName    String
  fieldType    String
  fieldComment String?
  isSelectable Boolean  @default(true)
  createdAt    DateTime @default(now())

  table DataSourceTable @relation(fields: [tableId], references: [id], onDelete: Cascade)

  @@map("table_fields")
}

model Knowledge {
  id        String   @id @default(uuid())
  name      String
  content   String   @db.Text
  category  String   // business, case_chat, case_workflow
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("knowledge")
}

model Workflow {
  id             String   @id @default(uuid())
  name           String
  description    String?  @db.Text
  datasourceId   String?
  flowDefinition Json     @db.Json
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  datasource DataSource? @relation(fields: [datasourceId], references: [id])

  @@map("workflows")
}

model Report {
  id           String   @id @default(uuid())
  userId       String
  title        String
  theme        String?
  datasourceId String?
  outline      Json?    @db.Json
  content      String?  @db.Text
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  user       User        @relation(fields: [userId], references: [id])
  datasource DataSource? @relation(fields: [datasourceId], references: [id])

  @@map("reports")
}

model Feedback {
  id        String   @id @default(uuid())
  userId    String
  messageId String?
  type      String   // positive, negative
  content   String?  @db.Text
  context   Json?    @db.Json
  createdAt DateTime @default(now())

  user    User     @relation(fields: [userId], references: [id])
  message Message? @relation(fields: [messageId], references: [id])

  @@map("feedbacks")
}

model MonitorMetric {
  id          String   @id @default(uuid())
  metricType  String   // user_count, session_count, message_count, report_count
  metricValue Int
  recordedAt  DateTime @default(now())

  @@map("monitor_metrics")
}
```

---

## 七、业务后端接口设计

### 7.1 RESTful API 规范

| 接口编号 | 接口名称 | 方法 | 路径 | 说明 |
|----------|----------|------|------|------|
| **用户管理** ||||
| API-001 | 用户登录 | POST | /api/auth/login | 用户认证 |
| API-002 | 用户登出 | POST | /api/auth/logout | 退出登录 |
| API-003 | 获取当前用户 | GET | /api/auth/me | 获取用户信息 |
| API-004 | 用户列表 | GET | /api/users | 分页获取用户 |
| API-005 | 创建用户 | POST | /api/users | 新增用户 |
| API-006 | 更新用户 | PUT | /api/users/:id | 修改用户 |
| API-007 | 删除用户 | DELETE | /api/users/:id | 删除用户 |
| **会话管理** ||||
| API-010 | 创建会话 | POST | /api/sessions | 新建会话 |
| API-011 | 会话列表 | GET | /api/sessions | 获取会话列表 |
| API-012 | 会话详情 | GET | /api/sessions/:id | 获取会话消息 |
| API-013 | 删除会话 | DELETE | /api/sessions/:id | 删除会话 |
| API-014 | 保存消息 | POST | /api/sessions/:id/messages | 保存消息 |
| **数据源管理** ||||
| API-020 | 数据源列表 | GET | /api/datasources | 获取数据源列表 |
| API-021 | 创建数据源 | POST | /api/datasources | 新增数据源 |
| API-022 | 数据源详情 | GET | /api/datasources/:id | 获取数据源详情 |
| API-023 | 更新数据源 | PUT | /api/datasources/:id | 修改数据源 |
| API-024 | 删除数据源 | DELETE | /api/datasources/:id | 删除数据源 |
| API-025 | 测试连接 | POST | /api/datasources/:id/test | 测试数据源连接 |
| API-026 | 获取表列表 | GET | /api/datasources/:id/tables | 获取数据表 |
| API-027 | 保存字段元数据 | PUT | /api/datasources/:id/fields | 保存字段配置 |
| **知识管理** ||||
| API-030 | 知识列表 | GET | /api/knowledge | 获取知识列表 |
| API-031 | 创建知识 | POST | /api/knowledge | 新增知识 |
| API-032 | 更新知识 | PUT | /api/knowledge/:id | 修改知识 |
| API-033 | 删除知识 | DELETE | /api/knowledge/:id | 删除知识 |
| API-034 | 同步向量库 | POST | /api/knowledge/sync | 触发同步 |
| **工作流管理** ||||
| API-040 | 工作流列表 | GET | /api/workflows | 获取工作流列表 |
| API-041 | 创建工作流 | POST | /api/workflows | 新增工作流 |
| API-042 | 工作流详情 | GET | /api/workflows/:id | 获取工作流详情 |
| API-043 | 更新工作流 | PUT | /api/workflows/:id | 修改工作流 |
| API-044 | 删除工作流 | DELETE | /api/workflows/:id | 删除工作流 |
| **报告管理** ||||
| API-050 | 报告列表 | GET | /api/reports | 获取报告列表 |
| API-051 | 创建报告 | POST | /api/reports | 新增报告 |
| API-052 | 报告详情 | GET | /api/reports/:id | 获取报告详情 |
| API-053 | 更新报告 | PUT | /api/reports/:id | 修改报告 |
| API-054 | 删除报告 | DELETE | /api/reports/:id | 删除报告 |
| **反馈管理** ||||
| API-060 | 反馈列表 | GET | /api/feedbacks | 获取反馈列表 |
| API-061 | 创建反馈 | POST | /api/feedbacks | 提交反馈 |
| API-062 | 删除反馈 | DELETE | /api/feedbacks/:id | 删除反馈 |
| API-063 | 导出反馈 | GET | /api/feedbacks/export | 导出 Excel |
| **服务监控** ||||
| API-070 | 监控指标 | GET | /api/monitor/metrics | 获取核心指标 |
| API-071 | 趋势数据 | GET | /api/monitor/trends | 获取趋势数据 |
| API-072 | 活跃用户 | GET | /api/monitor/active-users | 获取活跃用户 |

---

## 八、风险分析与应对

### 8.1 主要风险

| 风险项 | 影响程度 | 发生概率 | 应对措施 |
|--------|----------|----------|----------|
| AI 接口不稳定 | 高 | 中 | 1. 实现重试机制；2. 降级提示；3. 缓存历史结果 |
| SSE 连接中断 | 中 | 中 | 1. 自动重连机制；2. 断点续传；3. 状态恢复 |
| 高并发性能 | 高 | 中 | 1. 数据库连接池；2. 接口限流；3. 查询优化 |
| 跨域问题 | 低 | 低 | 1. Next.js API Route 代理；2. CORS 配置 |
| 大文件上传 | 中 | 低 | 1. 分片上传；2. 进度展示；3. 文件大小限制 |

### 8.2 AI 接口降级策略

```typescript
// lib/ai/fallback.ts
export async function withFallback<T>(
  primary: () => Promise<T>,
  fallback: () => T,
  options: { retries?: number; delay?: number } = {}
): Promise<T> {
  const { retries = 3, delay = 1000 } = options;

  for (let i = 0; i < retries; i++) {
    try {
      return await primary();
    } catch (error) {
      if (i === retries - 1) {
        console.error('AI 接口调用失败，使用降级方案');
        return fallback();
      }
      await new Promise(resolve => setTimeout(resolve, delay * (i + 1)));
    }
  }

  return fallback();
}
```

---

## 九、实施计划

### 9.1 阶段划分

| 阶段 | 内容 | 说明 | 预计周期 |
|------|------|------|----------|
| **Phase 1** | 项目初始化 | Monorepo 搭建、基础配置、shadcn/ui 集成 | 0.5 周 |
| **Phase 2** | 用户认证 | 登录/登出、权限控制、布局框架 | 0.5 周 |
| **Phase 3** | 数据源管理 | 数据源 CRUD、连接测试、表管理 | 1 周 |
| **Phase 4** | 智能问答 | WebSocket 对接、消息展示、流式输出 | 1.5 周 |
| **Phase 5** | 知识管理 | 知识 CRUD、案例库、向量同步 | 1 周 |
| **Phase 6** | 智能编排 | 流程编辑器、节点执行、SSE 对接 | 1.5 周 |
| **Phase 7** | AI 报告 | 大纲编辑、内容生成、富文本编辑 | 1.5 周 |
| **Phase 8** | 后台完善 | 服务监控、反馈管理、优化迭代 | 1 周 |
| **Phase 9** | 测试上线 | 集成测试、性能优化、部署上线 | 1 周 |

### 9.2 依赖关系

```
Phase 1 ──▶ Phase 2 ──▶ Phase 3 ──▶ Phase 4 ──▶ Phase 5
                │                        │
                │                        ▼
                │                   Phase 6 ──▶ Phase 7
                │
                └─────────────────────────────────▶ Phase 8 ──▶ Phase 9
```

**首期交付建议**：Phase 1-5，实现智能问答核心功能。

---

## 十、参考资料

- P1-需求文档：`docs/项目迭代/2026-03-17-[进行中]-智融数据问答平台/P1-需求文档.md`
- P2-需求评审报告：`docs/项目迭代/2026-03-17-[进行中]-智融数据问答平台/P2-需求评审报告.md`
- 原始需求梳理：`zhirong_需求梳理.md`
- Next.js 文档：https://nextjs.org/docs
- shadcn/ui 文档：https://ui.shadcn.com
- NestJS 文档：https://docs.nestjs.com
- Prisma 文档：https://www.prisma.io/docs
- Tailwind CSS 文档：https://tailwindcss.com/docs

---

*文档版本: v1.2*
*创建时间: 2026-03-17*
*更新时间: 2026-03-17*
*作者: Claude Code*
